// Copy this file to config.js and fill in the two values below.
// Find them in Supabase → Project Settings → API.
// config.js is the one actually loaded by index.html — keep it out of
// version control if this project is ever put under git.

window.SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co";
window.SUPABASE_ANON_KEY = "your-anon-public-key";
